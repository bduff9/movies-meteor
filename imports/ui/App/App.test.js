//import React from 'react';
//import { shallow } from 'enzyme';

//import App from './App.jsx';

describe('App', () => {
	/**
	 * @type {import('enzyme').ShallowWrapper<any, any, any>} wrapper
	 */
	//let wrapper;

	beforeEach(() => {
		//wrapper = shallow(<App />);
	});

	it('does nothing', () => {
		expect(true).toBe(true);
	});

	test.todo('add tests for App');
});
